# Default configuration for __NAME__.
#
# Profile-specific overrides go in config/{profile}.toml (e.g. config/local.toml);
# environment variables override files. The active profile comes from KLAUTHED_PROFILE
# (defaults to `local` in development).

[server]
host = "0.0.0.0"
port = 8080
// __IF db__

[database]
system = "__DB_SYSTEM__"
url = "__DB_URL__"
// __END db__
